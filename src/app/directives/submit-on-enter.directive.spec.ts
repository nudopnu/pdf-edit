import { SubmitOnEnterDirective } from './submit-on-enter.directive';

describe('SubmitOnEnterDirective', () => {
  it('should create an instance', () => {
    const directive = new SubmitOnEnterDirective();
    expect(directive).toBeTruthy();
  });
});
